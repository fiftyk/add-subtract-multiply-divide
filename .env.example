# =============================================================================
# fn-orchestrator 环境变量配置示例
# =============================================================================
# 复制此文件为 .env 并填入实际值
# 配置优先级: CLI 参数 > 环境变量 > .env 文件 > 默认值


# -----------------------------------------------------------------------------
# API 配置 (必需)
# -----------------------------------------------------------------------------

# Anthropic API Key (必需 - 二选一)
# 获取方式: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your-api-key-here

# Claude Code 兼容 (可选 - 如果已有此变量可不设置 ANTHROPIC_API_KEY)
# ANTHROPIC_AUTH_TOKEN=your-api-key-here

# 自定义 API 端点 (可选)
# 用于代理或自建 API 服务
# ANTHROPIC_BASE_URL=https://your-custom-endpoint.com


# -----------------------------------------------------------------------------
# LLM 配置 (可选)
# -----------------------------------------------------------------------------

# LLM 模型名称
# 默认: claude-sonnet-4-20250514
# LLM_MODEL=claude-sonnet-4-20250514

# 最大 token 数
# 默认: 1024
# LLM_MAX_TOKENS=1024

# LLM Adapter 类型（用于 Mock 代码生成）
# 默认: anthropic
# 可选值: anthropic (使用 Anthropic API), claude-code (使用本地 Claude Code CLI)
# 使用 claude-code 时无需 API Key，通过 claude-switcher 调用本地 CLI
# LLM_ADAPTER=anthropic


# -----------------------------------------------------------------------------
# 执行器配置 (可选)
# -----------------------------------------------------------------------------

# 步骤超时时间 (毫秒)
# 默认: 30000 (30秒)
# EXECUTOR_STEP_TIMEOUT=30000


# -----------------------------------------------------------------------------
# 存储配置 (可选)
# -----------------------------------------------------------------------------

# 数据存储目录
# 默认: .data
# STORAGE_DATA_DIR=.data


# -----------------------------------------------------------------------------
# Mock 自动生成配置 (可选)
# -----------------------------------------------------------------------------

# 启用/禁用 Mock 自动生成
# 默认: false (禁用)
# 支持值: true, false, 1, 0, yes, no, on, off (不区分大小写)
# AUTO_GENERATE_MOCK=false

# Mock 生成最大迭代次数
# 默认: 3
# 防止无限循环生成
# MOCK_MAX_ITERATIONS=3

# Mock 函数输出目录
# 默认: functions/generated
# MOCK_OUTPUT_DIR=functions/generated


# -----------------------------------------------------------------------------
# 日志配置 (可选)
# -----------------------------------------------------------------------------

# 日志级别
# 默认: info
# 可选值: debug, info, warn, error
# LOG_LEVEL=info


# =============================================================================
# 使用示例
# =============================================================================

# 开发环境 - 启用调试日志和 Mock 生成
# LOG_LEVEL=debug
# AUTO_GENERATE_MOCK=true

# 生产环境 - 禁用 Mock，使用真实函数
# AUTO_GENERATE_MOCK=false
# LOG_LEVEL=warn

# CI/CD 环境 - 快速验证流程
# AUTO_GENERATE_MOCK=true
# MOCK_MAX_ITERATIONS=1
# LOG_LEVEL=error
