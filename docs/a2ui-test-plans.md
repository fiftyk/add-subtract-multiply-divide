# A2UI 组件测试计划

本文档描述用于测试 A2UI v0.8 协议各种 UI 组件的 Test Plan。

---

## Plan 1: 数据展示类 (Data Display)

**目标**: 测试 Table、Card、Badge、Progress、Divider 组件

**Prompt**:
```
获取用户列表数据，然后获取订单统计信息，接着获取项目进度，再获取系统状态，最后生成分步报告
```

**预期函数调用**:
1. `getUserList()` → Table 组件
2. `getOrderStats()` → Card 组件
3. `getProjectProgress()` → Progress + Card 组件
4. `getSystemStatus()` → Badge 组件（多个）
5. `generateReport()` → 复合组件（Card + Divider + Table）

**测试的 UI 组件**:
| 组件 | 触发条件 |
|------|----------|
| Table | 返回数组结果 |
| Card | 返回对象结果 |
| Badge | status 字段值（healthy/warning/error/active/inactive） |
| Progress | percentage 字段（0-100） |
| Divider | 多个 Card/Section 之间 |

---

## Plan 2: 列表与布局 (List & Layout)

**目标**: 测试 List、Row、Column 组件

**Prompt**:
```
获取产品目录，然后获取待办事项列表，最后获取设置选项
```

**预期函数调用**:
1. `getProductCatalog()` → List 组件（嵌套）
2. `getTodoList()` → Card + List 或 CheckBox 组件
3. `getSettingsOptions()` → Card + Slider 组件

**测试的 UI 组件**:
| 组件 | 触发条件 |
|------|----------|
| List | 嵌套数组（categories + products） |
| Row | 水平布局 |
| Column | 垂直布局 |
| CheckBox | boolean 字段列表 |
| Slider | { value, min, max } 结构 |

---

## Plan 3: 用户注册表单 (User Registration Form)

**目标**: 测试 Text、Date、Single Select 输入字段

**Prompt**:
```
处理用户注册信息，用户名填写"testuser"，邮箱填写"test@example.com"，出生日期选择"1990-05-15"，国家选择"中国"
```

**预期函数调用**:
1. `processUserRegistration(...)` → 输入表单

**表单字段**:
| 字段名 | 类型 | 说明 |
|--------|------|------|
| username | text | 用户名 |
| email | text | 邮箱 |
| birthDate | date | 出生日期 |
| country | single_select | 国家选择 |

**测试的输入组件**:
- TextField (单行文本)
- DateTimeInput (日期)
- MultipleChoice / Select (单选)

---

## Plan 4: 订单查询表单 (Order Query Form)

**目标**: 测试 Number（带 range 验证）、Date、Select 输入字段

**Prompt**:
```
处理订单查询，订单号填写"ORD-2024"，开始日期选择"2024-01-01"，结束日期选择"2024-03-31"，最小金额输入0，最大金额输入1000，状态选择"completed"
```

**预期函数调用**:
1. `processOrderQuery(...)` → 输入表单

**表单字段**:
| 字段名 | 类型 | 验证 |
|--------|------|------|
| orderId | text | 可选 |
| startDate | date | 必填 |
| endDate | date | 必填 |
| minAmount | number | range: 0-10000 |
| maxAmount | number | range: 0-10000 |
| status | single_select | 选项: all/pending/processing/shipped/completed |

**测试的输入组件**:
- TextField (可选输入)
- DateTimeInput (日期范围)
- Number (带范围验证)
- Select (单选下拉)

---

## Plan 5: 调查问卷 (Survey Form)

**目标**: 测试 Multi Select、Boolean、Number、Text 输入字段

**Prompt**:
```
处理调查问卷回答，受访者填写"张三"，年龄填写25，感兴趣的领域选择"技术"和"产品"，经验水平选择"中级"，订阅资讯选择是，其他建议填写"希望增加更多功能"
```

**预期函数调用**:
1. `processSurveyResponse(...)` → 输入表单

**表单字段**:
| 字段名 | 类型 | 说明 |
|--------|------|------|
| name | text | 可选 |
| age | number | 必填 |
| interests | multi_select | 多选 |
| experienceLevel | single_select | 单选 |
| newsletter | boolean | Checkbox |
| comments | text (multiline) | 可选 |

**测试的输入组件**:
- TextField (单行/多行)
- Number
- MultipleChoice (多选)
- CheckBox (布尔值)

---

## Plan 6: 产品配置表单 (Product Configuration)

**目标**: 测试多种输入类型组合

**Prompt**:
```
配置产品选项，颜色选择"蓝色"，尺寸选择"大号"，数量输入3，礼品包装选择是，加急配送选择否，刻字内容填写"生日快乐"
```

**预期函数调用**:
1. `configureProductOptions(...)` → 输入表单

**表单字段**:
| 字段名 | 类型 |
|--------|------|
| color | single_select |
| size | single_select |
| quantity | number (1-10) |
| giftWrap | boolean |
| expressShipping | boolean |
| engraving | text (optional) |

---

## Plan 7: 高级搜索表单 (Advanced Search)

**目标**: 测试复杂多条件搜索表单

**Prompt**:
```
搜索并筛选数据，关键词填写"手机"，分类选择"电子产品"，最低价格输入100，最高价格输入5000，仅显示有货商品，排序方式选择"价格从低到高"
```

**预期函数调用**:
1. `searchAndFilter(...)` → 输入表单

---

## Plan 8: 会议预订表单 (Meeting Booking)

**目标**: 测试 DateTimeInput 和数组输入

**Prompt**:
```
预订会议，标题填写"项目讨论会"，日期选择"2024-04-15"，开始时间选择"14:00"，时长选择60分钟，会议室选择"301"，参会人员填写"a@example.com,b@example.com"
```

**预期函数调用**:
1. `bookMeeting(...)` → 输入表单

---

## Plan 9: 反馈提交表单 (Feedback Form)

**目标**: 测试 Rating (Number) 和多行 Text

**Prompt**:
```
提交反馈，反馈类型选择"功能建议"，评分选择4，详细反馈填写"希望增加导出功能到PDF"
```

**预期函数调用**:
1. `submitFeedback(...)` → 输入表单

---

## 运行测试

```bash
# 启动 web-server
cd packages/web-server && pnpm dev

# 启动 web-ui
cd packages/web-ui && pnpm dev

# 使用 CLI
npx fn-orchestrator plan "获取用户列表数据"
npx fn-orchestrator execute plan-xxx

# 或在 Web UI 中直接输入 prompt
```

---

## 预期 UI 效果

### Table 组件效果
```
┌────┬────────┬────────────────────┬─────────┬──────────┐
│ ID │  姓名  │       邮箱         │  状态   │   角色   │
├────┼────────┼────────────────────┼─────────┼──────────┤
│ 1  │  张三  │ zhangsan@...       │ active  │  admin   │
│ 2  │  李四  │ lisi@...           │ active  │  user    │
│ 3  │  王五  │ wangwu@...         │ inactive│  user    │
└────┴────────┴────────────────────┴─────────┴──────────┘
```

### Card + Progress 组件效果
```
┌─────────────────────────────────────────┐
│ 📊 项目进度                               │
├─────────────────────────────────────────┤
│ ┌─────────────────────────────────────┐ │
│ │ ████████████████████░░░░ 68%        │ │
│ │ 开发阶段 · 34/50 任务                 │ │
└─────────────────────────────────────────┘
```

### Badge 组件效果
```
✅ 数据库    ⚠️ 搜索引擎    ❌ 文件存储
```

### Form 组件效果
```
┌──────────────────────────────────────────────┐
│ 📝 用户注册                                  │
├──────────────────────────────────────────────┤
│ 用户名 *  [____________________]             │
│ 邮箱 *    [____________________]             │
│ 出生日期 * [📅 2024-03-15]                   │
│ 国家 *    [中国 ▼]                           │
│                                              │
│ [        提交        ]                       │
└──────────────────────────────────────────────┘
```
